---
description:
alwaysApply: true
enabled: true
updatedAt: 2026-07-29T06:19:42.634Z
provider:
---

1. 开口第一句叫我宝贝，始终用中文回复和编写文档
2. 实现始终考虑高性能，力求最小的内存最低的 cpu 占用，避免无意义的 clone，避免创建无意义的内存
3. 高可维护
4. 开发网络不通时尝试代理 `http://10.48.113.10:8080` 或 `http://192.168.31.5:1080` 或 `http://192.168.31.212:10808`
5. 修改完成并验证后自动提交和推送，如果仅是 git submodule 提交，父仓库可不同步处理；网络失败时使用代理
6. 终端和脚本只用 bash；Windows 用 MSYS2的ucrt64.exe 或bash.exe 或git bash.exe，禁止新增 .ps1
7. 将聊天中具有复用价值的用户纠正自动沉淀为项目规则
	- 用户在聊天过程中纠正实现方式、技术约束、工作流程或项目惯例时，判断该纠正是否对后续任务具有复用价值。
	- 可复用的纠正必须在本轮结束前提炼为简洁、可执行、无聊天上下文依赖的规则，并写入 `.agent/rules/` 下合适的现有规则或新规则。
	- 优先更新主题相同的现有规则；只有关注点独立时才新增 `.mdc`，避免重复、冲突和规则膨胀。
8. **禁止擅自改各扩展 `package.json` 的 `version`**（含 vscode-git / vscode-database）：开发迭代与打包保持 `0.0.1`；只有用户明确说「发布 / bump / 升版本」时才允许改版本号。功能进度写 CHANGELOG，不要用版本号当迭代计数器。