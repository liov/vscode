# vscode-httpclient

- **form-data** 必须按 `multipart/form-data`：用 `FormData` 发送，由运行时自动带 boundary；**禁止**手写无 boundary 的 `Content-Type: multipart/form-data`。
- form-data 的 cURL /「实际请求」用 `--form`（`-F`），**不要**写成 urlencoded 的 `--data-raw key=value&…`；文件字段用 `--form 'key=@path'`。
- form-data 字段类型：**Text** / **File**。File 只存本机 `filePath`/`fileName`（不写 base64 进 api.json）；发送时宿主读文件再 `FormData.append`。
- form 字段存盘为 JSON 数组时，插值必须**逐字段**做，禁止对整段 JSON 字符串做 `interpolate`（值含引号/反斜杠会弄坏 JSON）；文件路径不要插值。
- **urlencoded** 才是 `application/x-www-form-urlencoded`；与 form-data UI/文案要区分清楚。
- JSON 裸数字粘连 `103{{$timestamp}}`（插值为数字字符拼接）与 js-object 互转必须写成 `Number("103"+$timestamp)`，**禁止**用算术 `103+$timestamp`（语义不等价）。
- js-object ↔ XML：裸占位用 `bare="true"`；`bare` 内容一律还原为 BarePlaceholder（含 `103{{$ts}}`）；对象序列化一律包 `<root>`，避免单 key 丢字段名。
- 脚本三阶段：准备变量（插值前，无 `hc.request`）→ 内置插值 → 前置（已替换 request，不再自动重插）→ 发送 → 后置。
